CREATE TABLE `index` (
                         id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
                         page_id INT NOT NULL,
                         lemma_id INT NOT NULL,
                         `rank` FLOAT NOT NULL
);